apiVersion: v1
kind: Pod
metadata:
  name: resources
  labels:
    project: roboshop
    environment: v1
spec:
  containers:
    - name: nginx
      image: mginx
      resources:
        requests:
          memory: "68Mi"
          cpu: "100m"
        limits:
          memory: "100Mi"
          cpu: "120m"